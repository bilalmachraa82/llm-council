[build]
  command = "npm run build"
  publish = "dist"

# Handle SPA routing (React/Vite)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Set API URL for production
[context.production.environment]
  VITE_API_URL = "https://llm-council-backend-production.up.railway.app"
